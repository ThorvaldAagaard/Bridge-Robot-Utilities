# Build dependencies for Bridge-Robot-Utilities executables.
#
# Install into a clean virtual environment (see build.cmd) so that the
# generated executables only bundle what the utilities actually need. The
# clean venv is the real allowlist: packages that are not installed here
# (scipy, pandas, IPython/Jupyter, etc.) can never be bundled, regardless of
# what is installed globally on the build machine.
#
# Every version is pinned (a lockfile) so builds are reproducible across
# machines. To refresh after intentionally upgrading a package, rebuild the
# venv and run:  pip list --format=freeze
#
# pip and setuptools are intentionally not pinned here; they are provided by
# the virtual environment itself.

# --- Direct build dependencies ---
pyinstaller==6.20.0
endplay==0.5.10
numpy==2.4.6
colorama==0.4.6

# --- Transitive dependencies (pinned for reproducibility) ---
altgraph==0.17.5
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.10.9
more-itertools==11.1.0
packaging==26.2
pefile==2024.8.26
pillow==12.2.0
pyinstaller-hooks-contrib==2026.6
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pywin32-ctypes==0.2.3
six==1.17.0
tqdm==4.68.2
